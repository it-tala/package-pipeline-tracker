{
  "name": "tala-pipeline-dashboard",
  "version": "1.0.0",
  "private": true,
  "description": "Read-only Vercel dashboard for the Tala pipeline tracker (Google Sheets published CSV)",
  "scripts": {
    "dev": "npx serve ."
  }
}
